name: Java CI with Maven

on: [push, pull_request, workflow_dispatch]

jobs:
  build:

    runs-on: ubuntu-latest

    # Setup Matrix
    strategy:
      max-parallel: 2
      matrix:
        language_engine:
          - java
          - node

        include:
          - language_engine: java
            version: 1.8

          - language_engine: node
            version: 12

    # Checkout Repository Code
    steps:
      - uses: actions/checkout@v2

      # Setup Java and Node
      - name: Setup Java
        if: ${{ matrix.language_engine == 'java' }}
        uses: actions/setup-java@v1
        with:
          java-version: ${{ matrix.version }}

      - name: Setup Node
        if: ${{ matrix.language_engine == 'node' }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.version }}

      # Setup Caches
      - name: Cache local Maven repository
        if: ${{ matrix.language_engine == 'java' }}
        uses: actions/cache@v2
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Cache node modules
        if: ${{ matrix.language_engine == 'node' }}
        uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-

      # Run Tests
      - name: Test compile
        if: ${{ matrix.language_engine == 'java' }}
        run: mvn -B -V test-compile -DskipTests=true -Dmaven.javadoc.skip=true

      - name: Tests with Coveralls coverage reports
        if: ${{ matrix.language_engine == 'java' }}
        run: mvn clean test cobertura:cobertura jacoco:report coveralls:report -DdryRun=true -DtestClient

      # Send Coveralls Coverage Reports
      - name: Service Coveralls coverage report
        if: ${{ matrix.language_engine == 'java' }}
        uses: MikeEdgar/github-action@raw_coverage_file
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          flag-name: sage-${{ github.action }}-${{ github.run_number }}-${{ matrix.language_engine }}
          parallel: true
          path-to-file: ./target/coveralls.json
          coverage-format: raw

      - name: UI Coveralls coverage report
        if: ${{ matrix.language_engine == 'node' }}
        uses: MikeEdgar/github-action@raw_coverage_file
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          flag-name: sage-${{ github.action }}-${{ github.run_number }}-${{ matrix.language_engine }}
          parallel: true
          path-to-lcov: ./src/main/webapp/coverage/coveralls.json
          coverage-format: raw

      - name: Deploy to GitHub pages
        if: github.event.push.base.ref == 'master'
        uses: JamesIves/github-pages-deploy-action@3.7.1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BRANCH: gh-pages
          FOLDER: target/generated-docs
          CLEAN: true
          SINGLE_COMMIT: true

  # Resolve Coveralls Parallel
  finish:
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Coveralls Finished
      uses: MikeEdgar/github-action@raw_coverage_file
      with:
        github-token: ${{ secrets.github_GITHUB_TOKENtoken }}
        parallel-finished: true
